import { useState } from 'react'
import { useServerFn } from '@tanstack/react-start'
import { ThumbsDown,ThumbsUp,Users,Wrench } from 'lucide-react'
import { voteTip } from '../lib/manuauto.functions'
export type TipCardData={id:string;title:string;recommendation:string;engine:string;evidence?:string;status:string;confirmations:number;downvotes?:number}
export function TipCard({tip}:{tip:TipCardData}){const voteFn=useServerFn(voteTip),[up,setUp]=useState(tip.confirmations),[down,setDown]=useState(tip.downvotes??0),[myVote,setMyVote]=useState<boolean|null>(null),[busy,setBusy]=useState(false);async function vote(worked:boolean){setBusy(true);try{const result=await voteFn({data:{tipId:tip.id,worked}});setUp(result.upvotes);setDown(result.downvotes);setMyVote(worked)}finally{setBusy(false)}}return <article className="tip-card"><div className="tip-heading"><span className="community-badge"><Users size={13}/> Comunidade</span><strong>{tip.title}</strong></div><p>{tip.recommendation}</p>{tip.evidence?<p className="evidence"><Wrench size={13}/> Evidência informada: {tip.evidence}</p>:null}<div className="tip-meta">Motor/aplicação: {tip.engine} · {tip.status}</div><p className="disclaimer">Experiência de usuário, não procedimento de fábrica. A orientação oficial permanece válida.</p><div className="vote-row"><button className={myVote===true?'selected':''} disabled={busy} onClick={()=>vote(true)}><ThumbsUp size={14}/> Funcionou ({up})</button><button className={myVote===false?'selected':''} disabled={busy} onClick={()=>vote(false)}><ThumbsDown size={14}/> Não funcionou ({down})</button></div></article>}
