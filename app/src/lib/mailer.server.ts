import nodemailer from 'nodemailer'
export async function sendMail({to,subject,text}:{to:string;subject:string;text:string}){const host=process.env.SMTP_HOST;if(!host){if(process.env.NODE_ENV==='production')throw new Error('SMTP não configurado.');console.info(`[Manuauto e-mail local] Para: ${to}\n${subject}\n${text}`);return}const transport=nodemailer.createTransport({host,port:Number(process.env.SMTP_PORT||587),secure:process.env.SMTP_SECURE==='true',auth:process.env.SMTP_USER?{user:process.env.SMTP_USER,pass:process.env.SMTP_PASSWORD}:undefined});await transport.sendMail({from:process.env.EMAIL_FROM||'Manuauto <nao-responda@localhost>',to,subject,text})}
